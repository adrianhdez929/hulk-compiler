{
  function tan(x) => sin(x) / cos(x);
  print("Hello World");
  print(sin(2 * 4) ^ 2 + cos(3 * 4 / log(4, 64)));
  let a = 42 in if (a%2 == 0) print("Even") else print("odd"); 
  let a = 25 in print(if (a%2 == 0) "even" else "odd");
}
