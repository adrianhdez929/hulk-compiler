{
  type Point inherits A {
      x:Number = 0;
      y = 0;

      getX() {
      self.x;
    }
      getY() => self.y;

    setX(x, y, z) : Number => self.x = x;
      setY(y) => self.y = y;
  }

  
  let x = new Point() in {
    x.setX(5);
    print(x.getX());
  }
}